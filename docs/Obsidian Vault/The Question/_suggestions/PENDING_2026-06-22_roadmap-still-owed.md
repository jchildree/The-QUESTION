---
confidence: MEDIUM
source: the-question-session-2026-06-21/README.md
suggested: 2026-06-22
topic: roadmap/still-owed
---

# Suggestion: Roadmap -- Still Owed

Outstanding integration work from the 2026-06-21 session bundle, needing the live repo.

1. Splice the emission contract into `investigate` and `interrogate-spec` (provenance spec section 6).
2. Apply the provenance section to `BOARD.md` (`patches/board-provenance-section.md` is a splice, not a drop-in).
3. Verify the faceless mode-tracker handshake against `hooks/caveman-mode-tracker.js`.
4. Per `writing-skills` TDD: a RED baseline plus REFACTOR pass on each new skill before retiring `grill-me`, `diagnose`, `caveman`, `caveman-commit`, `caveman-review`.

## Why

From the README "Still owed" section. Tracks the four open tasks gating the merge of staged work into the live plugin tree. MEDIUM: a session-specific TODO list, not a stable spec; recheck against repo state before acting (the board provenance splice and emission contract may already be partially done in a later session).
